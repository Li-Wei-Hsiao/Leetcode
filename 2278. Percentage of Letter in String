class Solution {
    public int percentageLetter(String s, char letter) {
        int kk = 0;
        for (int i = 0; i < s.length(); i++)
        {
            if (s.charAt(i) == letter)
                kk ++;
        }
        return(kk * 100 / s.length());
    }
}
