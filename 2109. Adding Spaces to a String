class Solution {
    public String addSpaces(String s, int[] spaces) {
        StringBuffer kk = new StringBuffer(s);
        for (int i = spaces.length - 1; i >= 0; i --)
            kk.insert(spaces[i], " ");
        
        return kk.toString();
    }
}
